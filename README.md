This repository contains the temporary under-construction version of the `vspcoderz.github.io` site.

## Getting Started

Install dependencies and run the development server with npm:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) to view the site.

To create a production build:

```bash
npm run build
```
